spring:
  application:
    name: indiene-api
  datasource:
    url: jdbc:postgresql://localhost:5432/indiene
    username: indiene
    password: indiene
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
